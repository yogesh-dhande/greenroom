/**
 * Canonical gallery alias. Keep `/speakers` for existing links while exposing
 * the product's headshot-forward surface at the route attendees and embed
 * consumers naturally look for.
 */
export { default } from "../speakers/page";
